// Get the current year
const currentYear = new Date().getFullYear();

// This updates the content of the span with id "currentYear" with the current year
document.getElementById('currentYear').textContent = currentYear;
